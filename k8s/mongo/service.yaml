apiVersion: v1
kind: Service

metadata:
  name: mongo-service
  namespace: memories-prod

spec:

  clusterIP: None

  selector:
    app: memories-mongo

  ports:

  - port: 27017

    targetPort: 27017