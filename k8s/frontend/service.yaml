apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: memories-prod
  
spec:

  type: NodePort

  selector:
    app: memories-frontend

  ports:

  - port: 80

    targetPort: 80

    nodePort: 30080